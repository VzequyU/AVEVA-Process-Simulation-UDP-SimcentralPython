# Integration tests

The local `test_aveva_engine.py` has not yet been inspected or imported. APS integration tests require the configured Windows simulator, model, snapshot and DLL. Do not run them as ordinary offline CI tests or treat them as read-only without reviewing their behavior.
